services:
  - type: web
    name: permitvista-frontend
    env: static
    staticPublishPath: ./
    buildCommand: echo "Static site - no build required"
    startCommand: python3 -m http.server ${PORT:-10000}

